### 2. 공통 한자어 비율을 계산
두 문단 간의 유사도는 **한자어 자카드 유사도(Jaccard similarity)** 로 측정하였다. 계산 방식은 직관적이다. 두 문단에 등장하는 한자어를 각각 모은 뒤, 겹치는 한자어의 수를 전체 한자어 종류의 수로 나눈다. 겹치는 한자어가 많을수록 값이 높아지고, 완전히 같으면 1, 전혀 겹치지 않으면 0이 된다. 수식으로 표현하면 J(A, B) = |A ∩ B| / |A ∪ B|이다.

1915 텍스트의 626개 문단과 1924 텍스트의 365개 문단을 전수 비교하여, 총 **228,490개 문단 쌍**의 유사도를 산출하였다. 임계값(유의미한 유사성을 인정하는 최소 기준) 0.1의 타당성은 통계적 근거와 의미론적 근거 양쪽에서 확인할 수 있다.

**&lt;표 3&gt; 228,490개 문단 쌍의 유사도 요약 통계**

<!-- component: stats_table -->

&lt;표 3&gt;에서 보듯이, 228,490개 쌍의 평균 유사도는 0.005에 불과하다. 임계값 0.1은 이 평균의 **20배**에 달하는 값이다. 실제로 22만 8천여 개 쌍 중 유사도 0.1을 넘는 경우는 **135개(0.06%)** 에 불과했다.

의미론적으로도 0.1은 충분히 엄격한 기준이다. 자카드 공식을 정리하면, 유사도 0.1을 달성하기 위해 필요한 공통 한자어 수는 |A ∩ B| = (|A| + |B|) / 11로 계산된다.

**&lt;표 4&gt; 유사도 0.1 달성에 필요한 공통 한자어 수**

<!-- component: common_tokens_table -->

평균적인 문단 크기(1915: 40종, 1924: 32종)에서 유사도 0.1을 달성하려면, **최소 7종의 동일한 한자어**가 두 문단에 공통으로 등장해야 한다. '宗敎', '世界' 같은 범용어 1~2개만 공유해서는 0.1에 도달할 수 없다. '唯物論'(유물론), '唯心論'(유심론), '實在論'(실재론) 같은 **특정 주제의 전문 용어**가 여러 개 겹쳐야 비로소 0.1에 이른다.

실제 사례를 보면, 유사도 0.10 근처의 참조쌍에서도 의미 있는 개념어 공유가 확인된다. 예를 들어, Rank 111(유사도 0.1042)에서는 1915 문단의 8종 한자어와 1924 문단의 10종 한자어 중 {佛敎, 基督敎} 2종이 공통으로 등장하며, Rank 113(유사도 0.1026)에서는 양쪽 문단 각각 9종 한자어 중 {一神敎, 超人的} 2종이 공유된다. 이처럼 유사도 0.1 근처에서도 '一神敎', '超人的' 같은 **종교학 핵심 용어**가 공유되어야 도달 가능하다.

임계값 0.1 이상인 135개 쌍을 초기 후보로 선정하였다. 이들의 유사도 분포는 &lt;표 5&gt;와 같다.

**&lt;표 5&gt; 유사도 0.1 이상 135개 쌍의 구간별 분포**

<!-- component: range_table -->

그러나 유사도 0.1 이상이더라도, 공유하는 한자어가 **내용적으로 무의미한 경우**가 있다. 이를 노이즈로 분류하여 제외하였다. 노이즈는 두 유형으로 구분된다. 첫째, **서수만 공유하는 경우**(7개)로, 두 문단이 '第一', '第二', '第三' 등 번호 표기만 공유하고 내용상 핵심 개념어는 공유하지 않는 경우이다. 예컨대 Rank 69의 경우, 1915 문단에는 "第一に..."가, 1924 문단에는 "第一 儒敎와..."가 포함되어 있어 {第一}만 공유할 뿐 실질적 내용 연관은 없다. 둘째, **범용어만 공유하는 경우**(17개)로, '宇宙', '世界', '如何', '對照', '區別' 등 특정 학술 맥락을 지시하지 않는 범용어만 공유하는 경우이다.

**&lt;표 6&gt; 노이즈 필터링 결과**

<!-- component: valid_pairs_table -->

이러한 노이즈를 제외한 결과, **111개 유효 참조쌍**이 확정되었다. 이제 이 111개 쌍이 텍스트 안에서 어떻게 분포하는지를 살펴볼 차례다.

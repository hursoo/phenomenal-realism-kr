# 원본 이미지와 PDF

이 저장소는 페이지 이미지 1,122장(『철학과 종교』 832장 · 『인내천요의』 290장)과 1924년 원본
PDF를 싣고 있다. **다만 1915년 원본 PDF는 싣지 않았다.** 231MB로 GitHub의 파일당 100MB 제한을
넘기 때문이다.

이 문서는 그것을 어떻게 구하고 어떻게 이 저장소의 이미지와 같은 상태로 만드는지 적는다.

---

## 1. 『哲學と宗敎』(1915) 원본 PDF

### 받는 곳

**일본 국립국회도서관(NDL) 디지털컬렉션**

```
https://dl.ndl.go.jp/pid/952938
```

저작권 보호기간이 만료된 자료이므로 **허락 없이 이용·전재할 수 있다.** NDL은 2017년부터 보호기간
만료 인터넷 공개 자료의 자유 이용을 명시하고 있다. 출처 표시는 권장 사항이다.

받은 파일은 약 231MB이며, 이 저장소가 쓴 것은 `0_哲学と宗教_952938_0001.pdf`라는 이름으로
저장된 판이다.

### 저장 위치

```
data/1_raw_data/philo_relig_1915.pdf
```

`.gitignore`가 이 경로를 제외하고 있다. 받아서 그 자리에 두면 아래 절차가 그대로 돈다.

---

## 2. 페이지 이미지 만들기

원본은 **펼침면(見開き)** 상태다. 두 쪽이 한 장에 들어 있고 주변에 배경 여백이 있다. 세
스크립트가 차례로 처리한다.

```bash
python scripts/01_make_one_sheet.py      # PDF → 낱장 이미지
python scripts/02_split_two_side.py      # 펼침면 → 좌·우 분할
python scripts/03_cut_extra_space.py     # 배경 여백 제거
```

| 스크립트 | 하는 일 |
|---|---|
| `01_make_one_sheet.py` | PDF에서 낱장 이미지를 뽑는다 |
| `02_split_two_side.py` | 그레이스케일 → 가우시안 블러 → **Otsu 이진화 → 윤곽선 검출**로 책 영역을 찾고 좌·우로 가른다 |
| `03_cut_extra_space.py` | 남은 배경 여백을 잘라낸다 |

결과는 `data/2_cut_renumbering/PR_cut_renumber/`에 **832장**이다.

**이 단계는 결정론적이다.** 같은 원본에서 같은 결과가 나오므로, 저장소에 이미 실려 있는 이미지와
대조해 확인할 수 있다.

---

## 3. 『人乃天-要義-』(1924)

원본 PDF(15.6MB)와 페이지 이미지 290장이 모두 저장소에 있다.

```
data/1_raw_data/innae_yoeui_1924.pdf
data/2_cut_renumbering/IY_cut_renumber/
```

저본은 편자 소장 영인본을 직접 촬영한 것이다. 같은 내용이 韓國學文獻硏究所 편,
『東學思想資料集』 3(아세아문화사, 1979)에 수록되어 있다.

---

## 4. 왜 이미지를 남기는가

판독 결과를 검증하려면 원본을 봐야 한다. 특히 이 코퍼스는 **멀티모달 언어모델이 판독한 것**이라
오독이 깨진 글자가 아니라 문맥에 맞는 다른 글자로 나타난다
([`docs/06_ai_disclosure.md`](docs/06_ai_disclosure.md)).

**중요한 인용은 반드시 이미지와 대조하기를 권한다.** `page_info` 열이 원본 쪽 번호를 담고 있으므로
해당 이미지를 곧바로 찾을 수 있다.

---

## 5. 판권지

두 책의 판권지 이미지는 별도로 두었다.

```
data/1_raw_data/colophon/
```

서지 실측의 근거이며, 판독 내용은 [`docs/01_sources.md`](docs/01_sources.md)에 있다.
